# EduTrack — Entity Relationship Diagram

> Source of truth: the JPA entities in
> `backend/e-journal/src/main/java/com/edutrack/edutrack/entity/`.
> Tables are generated by Hibernate (`spring.jpa.hibernate.ddl-auto=update`);
> `sql/create-tables.sql` is **deprecated** and slightly out of date.

```mermaid
erDiagram
    roles ||--o{ users : "has"

    users ||--o| teachers : "is-a"
    users ||--o| students : "is-a"
    users ||--o| schools  : "directs"
    users ||--o{ students : "parent-of"
    users ||--o{ school_events : "created"

    schools ||--o{ classes : "has"
    schools ||--o{ teachers : "employs"
    schools ||--o{ students : "enrolls"
    schools ||--o{ schedules : "has"
    schools ||--o{ school_profiles : "offers"
    schools ||--o{ school_events : "hosts"
    schools ||--o{ school_schedule_entries : "defines"
    schools ||--o| school_term_configs : "configured-by"

    classes ||--o{ students  : "contains"
    classes ||--o{ schedules : "scheduled-in"
    classes ||--o{ school_events : "targets"

    school_profiles ||--o{ students : "tracks"

    subjects ||--o{ schedules : "taught-as"
    subjects ||--o{ teacher_qualifications : "qualifies"
    teachers ||--o{ teacher_qualifications : "qualified-in"

    teachers ||--o{ schedules : "teaches"

    schedules ||--o{ grades     : "graded-in"
    schedules ||--o{ absences   : "absence-in"
    schedules ||--o{ complaints : "complaint-in"

    students ||--o{ grades     : "receives"
    students ||--o{ absences   : "has"
    students ||--o{ complaints : "subject-of"

    roles {
        int    id PK
        string name UK
    }

    users {
        int    id PK
        string email UK
        string passwordhash
        string firstname
        string lastname
        int    roleid FK
        bytea  profile_picture
        string profile_picture_type
        string bio
    }

    schools {
        int    id PK
        string name
        string address
        string type "enum SchoolType"
        int    directorid FK "-> users (one director)"
        int    student_limit
    }

    classes {
        int    id PK
        string name
        string schoolyear
        int    schoolid FK
    }

    teachers {
        int     userid PK "= users.id (@MapsId)"
        int     schoolid FK "nullable"
        decimal salary
    }

    students {
        int userid PK "= users.id (@MapsId)"
        int schoolid FK "nullable"
        int classid FK "nullable"
        int profileid FK "nullable"
        int parentid FK "nullable -> users"
    }

    subjects {
        int    id PK
        string name UK
    }

    teacher_qualifications {
        int teacherid PK "FK -> teachers"
        int subjectid PK "FK -> subjects"
    }

    schedules {
        int    id PK
        int    schoolid FK
        int    classid FK
        int    subjectid FK
        int    teacherid FK
        int    term "1 or 2"
        int    dayofweek "1=Mon..5=Fri"
        time   starttime
        time   endtime
        string lecture_type "enum LectureType"
        bool   track_attendance
    }

    grades {
        int       id PK
        int       studentid FK
        int       scheduleid FK
        decimal   value "2.00..6.00"
        timestamp createdat
    }

    absences {
        int  id PK
        int  studentid FK
        int  scheduleid FK
        date date
        bool isexcused
    }

    complaints {
        int    id PK
        int    studentid FK
        int    scheduleid FK
        string description
        date   date
    }

    school_events {
        int       id PK
        string    title
        string    description
        date      date
        string    type "enum EventType"
        int       schoolid FK
        int       classid FK "nullable"
        int       created_by FK "-> users"
        timestamp created_at
    }

    school_profiles {
        int    id PK
        int    schoolid FK
        string name
    }

    school_schedule_entries {
        int    id PK
        int    schoolid FK
        string type "LECTURE | BREAK | SPECIAL_EVENT"
        string label
        time   start_time
        time   end_time
        date   event_date "only for SPECIAL_EVENT"
        int    sort_order
    }

    school_term_configs {
        int id PK
        int schoolid FK "one-to-one"
    }
```

## Notes

- **`teachers` / `students` share their PK with `users`** via JPA `@MapsId`
  (`userid` is both PK and FK → `users.id`). They are "is-a" extensions of a user.
- **`users.parentid`**: a STUDENT row points to a parent **user** (not a separate
  parents table) through `students.parentid`.
- **`schools.directorid`** is the headmaster (a `users` row); one-to-one.
- **`teacher_qualifications`** is the join table for the many-to-many between
  `teachers` and `subjects`.
- A `schedule` is the central link: `grades`, `absences`, and `complaints` all
  reference both a `student` and a `schedule`.
